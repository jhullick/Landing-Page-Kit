---
description: Critique the current page for generic AI-default design and replace the worst offenders.
---
Look at the landing page you just built. Where is it pattern-matching to the most common
coming-soon template in your training data? Identify three specific elements that feel generic,
derivative, or like a default (fonts, layout, colour, hero composition, button style). Replace
each with a choice that feels distinctively like this brand and no one else, staying within the
brand tokens in CLAUDE.md. Implement the replacements, then tell me what you changed and why.
