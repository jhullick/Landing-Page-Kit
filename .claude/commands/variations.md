---
description: Generate three distinct variations of a section, changing only that section.
argument-hint: [section, default hero]
---
Produce three versions of the $ARGUMENTS section (default: the hero), changing only that section:
(1) minimal and typographic, (2) image or illustration-led, (3) split layout with the signup form
prominent. Keep the brand tokens and the rest of the page identical across all three. Save them as
separate files or variants so I can compare them side by side.
